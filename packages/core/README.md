# react-router-xray-core

Feature-gated Rust core for `react-router-xray`.

- `cli` builds the `xray` native binary.
- `wasm` exposes wasm-bindgen exports for web consumers.
- `napi` exposes Node-API bindings for JS tooling.
