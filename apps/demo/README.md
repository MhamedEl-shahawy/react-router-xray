# Demo App

Vite + React + React Router v6 demo integrating:

- `vite-plugin-react-router-xray` at startup.
- `react-router-xray/react` overlay at runtime.
- `pnpm analyze` script running the `xray` CLI.
